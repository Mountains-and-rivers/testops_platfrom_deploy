# ============================================================
# GitLab 远程 Redis / PostgreSQL 连接配置
# 默认 REMOTE=false 使用本地服务（127.0.0.1）
# 设为 REMOTE=true 后连接远程独立部署的 PG/Redis
# ============================================================
REMOTE=false

# ── PostgreSQL 远程连接 ──
PG_HOST=127.0.0.1
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=Pg1@zendao2024

# ── Redis 远程连接 ──
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=Pg1@zendao2024
