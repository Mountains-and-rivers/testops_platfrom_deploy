# K8s Secret — 数据库密码等敏感信息
apiVersion: v1
kind: Secret
metadata:
  name: zentao-secret
  namespace: ${NAMESPACE}
type: Opaque
stringData:
  mysql-user: "${MYSQL_USER}"
  mysql-password: "${MYSQL_PASSWORD}"
  mysql-database: "${MYSQL_DATABASE}"
