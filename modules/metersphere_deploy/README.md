# MeterSphere Deploy — MeterSphere 测试平台部署模块（规划中）

## 概述

自动化部署 MeterSphere 开源持续测试平台到 K8s 集群。

## 状态

**规划中** — 目录结构将完全复用 `k8s_cluster_deploy` 模板。

## 计划支持

- MeterSphere 最新稳定版
- MySQL 数据库
- Kafka / Redis 中间件
- 持久化存储（PV/PVC）
- Ingress 域名访问
